export default interface Metadata {
  id: string
  sequence: number
  postTitle: string
  createdDate: string
  shortNl: string
  shortEn: string
  tags: string
  thumbnail: string
  visible: boolean
}
